import { HomeScene } from "@/components/HomeScene";

/**
 * Renders inside a 1440×1624 iframe so viewport units match the design comp.
 */
export default function InternalHomePage() {
  return <HomeScene />;
}
