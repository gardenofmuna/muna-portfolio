/**
 * Frozen desktop composition size. The viewport cover-scales this rect (no stretch);
 * when the window aspect ratio differs, the scaled artboard is centered and cropped.
 */
export const ARTBOARD_WIDTH = 1440;
export const ARTBOARD_HEIGHT = 1624;
